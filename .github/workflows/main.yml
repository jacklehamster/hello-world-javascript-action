on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello world action step
        id: hello
        uses: jacklehamster/hello-world-javascript-action@v1.01
        with:
          who-to-greet: ${{ github.repository}}
