name: 'Dobuki Directory generator'
description: 'Generates a directory of the files in your repo'
branding:
  icon: 'sun'  
  color: 'yellow'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
  file:
    description: 'File to modify'
    default: 'package.json'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'node16'
  main: 'index.cjs'
